<% _.forEach(images, function(image) { %><%= '.' + image.selector %> { background: url(<%= image.url %>) no-repeat 0 0; width: <%= image.dimensions.width %>px; height: <%= image.dimensions.height %>px; }<%= '\n' %><% }); %>